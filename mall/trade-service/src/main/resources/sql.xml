<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lonelyash.mapper.AddressMapper">

    <select id="" parameterType="int" resultType="String">
        select name from mall.cart
        left join mall.item on cart.item_id = item.id;
    </select>


</mapper>

