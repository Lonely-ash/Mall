server:
  port: 8085
mall:
  swagger:
    title: 交易服务接口文档
    package: com.lonelyash.trade.controller
  db:
    database: mall-trade
feign:
  okhttp:
    enabled: true # 开启OKHttp连接池支持
  sentinel:
    enabled: true # 开启Feign对Sentinel的整合